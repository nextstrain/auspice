import React from "react"; // eslint-disable-line import/no-extraneous-dependencies
import { Container, Leader } from "./styles";


const Header = () => (
  <Container>
    <Leader>
      This page ('auspice-ex-avibus') represents a prototype of a interactive
      in-browser narratives editor. To begin, click the botton below to load
      an example narrative which you can then edit
    </Leader>
  </Container>
);
export default Header;

