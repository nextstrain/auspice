### App specific ###
# The published package ships the transpiled `cli-build/` instead, because Node
# refuses to strip types from files under `node_modules/`. `cli-entry.js` uses the
# absence of `cli/` to decide which of the two to load.
/cli/
big_flu
/data/
/releases/
/static/
s3/
/local_narratives/
/narratives/
/datasets/

### test artefacts ###
test/fetched-jsons/
.playwright/

### OSX ###
.DS_Store

### npm ###
npm-debug.log
