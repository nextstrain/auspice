/* Must stay in lockstep with the version in package.json.
 * .github/workflows/release.yaml checks this, and updates both when releasing.
 */
const version = "2.73.0";

module.exports = {
  version
};
